#!/bin/bash

HOSTNAME="`hostname`"
TOTAL="`wmctrl -l | grep "${HOSTNAME} .* - YouTube - Chrome" -o`"

NOPREFIX=${TOTAL#${HOSTNAME} }
NOSUFFIX=${NOPREFIX% - YouTube - Chrome}
TITLE=${NOSUFFIX}

echo $TITLE
